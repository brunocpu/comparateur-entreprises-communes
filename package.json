{
  "name": "comparateur-entreprises-communes",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tells Node to treat js/*.js as ES modules. The browser app does not read this file.",
  "scripts": {
    "test": "node --test test/units.mjs",
    "test:e2e": "node test-pull.mjs",
    "build:data": "node scripts/build-data.mjs",
    "build:og": "node scripts/build-og-image.mjs",
    "build:sw-version": "node scripts/compute-sw-version.mjs"
  },
  "devDependencies": {
    "@fontsource/fraunces": "^5.2.9",
    "@fontsource/geist-mono": "^5.2.7",
    "@fontsource/geist-sans": "^5.2.5",
    "@resvg/resvg-js": "^2.6.2",
    "wawoff2": "^2.0.1"
  }
}
